# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
htmlcov/
.coverage

# Audio output artifacts and render scratch space.
# The whole tree is ignored, not just the audio: DT-94/DT-95 write renders and
# per-run report.json here, and anything derived from the D-029 private corpus
# (including its measurements) is local/internal-only and must never be
# committed. Committed evidence goes to reports/ after the anonymization gate.
output/

# Dataset governance (docs/data/DATASET_GOVERNANCE.md): raw/restricted/derived
# audio never enters git; only data/manifests/ and data/licenses/ are tracked.
data/local/
data/restricted/
data/derived/
# Artist consent forms contain PII: only the index is tracked (Tier P).
data/licenses/artist/*
!data/licenses/artist/INDEX.md

# CI regression artifacts (written on failure)
artifacts/

# Agent/orchestrator internals
.agent/
.autoclaw/

# Environment
.env
.env.local

# Private paired-corpus per-run reports (titles/metrics) stay local
reports/evaluations/paired-corpus/2*/
